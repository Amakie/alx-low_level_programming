#include <stdio.h>
#include "main.h"
#include <stdlib.h>

/**
 * create_array - creates an array
 * @c: character to the pointer
 * @size: size of the unsigned integer
 *
 * Return: NULL
 */

char *create_array(unsigned int size, char c)
{
	char *ptr[];
	unsigned int size = n;

	if (size == 0)
	{
		malloc(n * sizeof(char c));
		return (NULL);
	}
	else
	{
		return (ptr);
	}
}
