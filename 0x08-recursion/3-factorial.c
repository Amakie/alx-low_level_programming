#include "main.h"
/**
 * factorial - calculates a factorial of a number
 * @n: input variable
 * Return: Always 0
 */
int factorial(int n)
{
	if (n > 0)
	{
		_putchar(n * (n - 1);
		n++;
	}
	else
	{
		return (-1);
	}
}
