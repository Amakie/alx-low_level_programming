#include <stdio.h>

/**
 * main - adds positive numbers
 * @argc: counts number of argument
 * @argv: arrays to the arguments
 *
 * Return: Always 0
 */
int main(int argc, char *argv[])
{
	int num1;
	int num2;

	num1 = atoi(argv[1]);
	num2 = atoi(argv[2]);
	sum = num1 + num2
	{
		printf("%d\n", sum);
	}
	if (argc == 1)
	{
		printf("0\n");
	}
	if (argv ![num1, num2])
	{
		printf("Error\n");
		return (1);
	}
	return (0);
}
