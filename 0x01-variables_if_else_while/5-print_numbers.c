#include <studio.h>
#include <stdlib.h>
/**
 * main - single digits
 *
 * Return: Always 0
 */
int main(void)
{
	int single;

	for (single = 0; single < 10; single++)
		printf("%d", single);
	printf("\n");
	return (0)
}
