#include <stdlib.h>
#include <time.h>
#include <stdio.h>
/**
 *
 * Return: Always 0
 */
int main(void)
{
	int n;

	srand(time(0));
	n = rand() - RAND_MAX / 2;
	if (n>5)
		printf("Last digit of %d is": %d", num, digit")
	return (0);
}
