#include "main.h"
/**
 * _abs - compute absolute value of an integer
 * @i: integer to be computed
 *
 * Return: Absolute value.
 */
int _abs(int i);
{
	if (x >= 0)
		return (i);
	else
		return (-i);
}
