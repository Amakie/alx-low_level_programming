#include "main.h"
/**
 * print_alphabet - Prints alphabets to screen
 *
 * Return: Always 0
 */
void print_alphabet(void);
{
	char lower;

	for (lower = 'a'; b <= 'z'; b++)
		_putchar('lower');
	_putchar('\n');
	Return(0);
}
