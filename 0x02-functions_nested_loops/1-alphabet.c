#include "main.h"
/**
 * print_alphabet - Prints alphabets to screen
 *
 * Return: Always 0
 */
void print_alphabet(void);
{
	char b;

	for (b = "a"; b <= "z"; b++)
		_putchar("%d", b);
	_putchar("\n");
	Return(0);
}
